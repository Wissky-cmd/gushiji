# 简历素材：黑马记账项目（秋招·开发岗版）

> 定位：计算机专业 + 秋招开发岗。本项目在简历中的价值 = 证明「**工程落地能力 + AI 工具驾驭能力**」。
> 写法原则：技术细节敢写（面试必须能讲），代码来源诚实（AI 辅助生成）。

---

## 0. 诚信边界（务必先读）

✅ **可以放心写：**

- 技术选型决策：对比 Electron、Tauri、Flutter 三套方案的成熟度、体积、维护成本后拍板
- 方案决策：SQLite 本地存储（隐私 + 零服务器成本）、数据 CSV 备份、两级分类模型
- 全流程交付：需求分析 → 迭代开发 → 验收测试 → 打包发布 v1.0
- AI 辅助开发（Claude Code）：任务拆解、驱动实现、分阶段验收、缺陷反馈闭环
- 真实缺陷的发现与修复：Excel 日期格式、文件占用报错、环境变量启动失败

❌ **不要写：**

- "独立编写全部代码"——技术面大概率让现场写或追问细节
- "精通 Electron/Vue/TypeScript"——写"了解/熟悉"，程度按你的真实水平

💡 **2026 年的语境**：AI 辅助开发已是行业主流（大厂普遍在用 Copilot/Claude），诚实写不减分；减分的是"只会让 AI 生成、讲不清技术细节"。所以——**投简历前，把第 4 节的技术要点逐个看懂，那才是你的真弹药**。

---

## 1. 项目经历（开发岗版，可直接复制）

**黑马记账 —— 桌面记账应用（Windows）**｜2026.09｜独立开发（AI 辅助）

- 独立完成桌面记账应用全流程交付：需求分析 → 技术选型 → 迭代开发 → 验收测试 → 打包发布（v1.0 Windows 安装包）
- 技术选型：对比 Electron、Tauri、Flutter 三套桌面方案，综合成熟度、体积与维护成本选定 Electron 39 + Vue 3 + TypeScript + Vite
- 系统设计：主进程/渲染进程分离架构，preload 脚本经 contextBridge 安全暴露 IPC 接口；SQLite 本地存储，金额以"分"为整数单位存储避免浮点误差；实现两级分类管理、月度统计图表（ECharts）与 CSV 导出备份
- 使用 AI 编程助手（Claude Code）驱动全部代码实现（数千行），负责方案设计、分阶段验收与缺陷修复推动；以用户视角发现并修复 Excel 日期格式错乱、文件占用等缺陷
- 工程实践：Git 版本管理、electron-builder/NSIS 打包发布、5 阶段迭代交付
- 技术栈：Electron、Vue 3、TypeScript、Vite、Element Plus、ECharts、SQLite、electron-builder

## 2. 精简版（简历空间紧张时用）

**黑马记账 —— Electron 桌面记账应用**｜2026.09｜独立开发（AI 辅助）

- 独立完成需求、选型、开发、验收、发布全流程，交付 v1.0 Windows 安装包
- Electron + Vue 3 + TypeScript + SQLite；主进程/渲染进程架构与 IPC 通信，金额整数存储避免浮点误差
- 使用 Claude Code 驱动代码实现，负责方案设计、验收测试与缺陷修复；5 阶段迭代交付

---

## 3. 技能清单（措辞按你的真实水平调整）

| 技能 | 写法建议 |
|---|---|
| AI 辅助开发 | 熟练使用 Claude Code 完成从需求到发布的全流程：任务拆解、驱动实现、验收测试、缺陷反馈闭环 |
| 前端 | 了解 Vue 3 / TypeScript（**面试会被追问，按真实水平写"了解/熟悉"**） |
| 桌面端 | 了解 Electron 架构：主进程、渲染进程、preload、IPC 通信 |
| 数据库 | 了解 SQLite：表结构设计、外键、索引、事务、WAL 模式 |
| 工程工具 | Git、npm、electron-builder 打包发布流程 |

---

## 4. 面试深挖准备（秋招技术面必看，都是本项目的真实细节）

| 问题 | 回答要点（先看懂再上场） |
|---|---|
| **Electron 进程模型？** | 主进程（Node 环境，管窗口/数据库/文件）、渲染进程（浏览器环境，只跑界面）、preload 桥接两者；通过 contextBridge 暴露受限 API，不把 Node 能力直接给界面，安全 |
| **数据怎么设计的？** | 两张表：categories（两级分类，parent_id 自关联）、records（账目，category_id 外键）；金额用 amount_cents 整数"分"存储，避免浮点误差；date 建索引；WAL 模式提升并发读写 |
| **为什么不用 better-sqlite3？** | 用 Electron 39 自带的 node:sqlite，免原生模块编译，减少安装门槛；API 满足需求 |
| **为什么选 Electron 不选 Tauri？** | 成熟稳定、生态大、问题好排查；Tauri 体积小但需 Rust 工具链、首次编译慢；记账场景对安装包体积不敏感 |
| **遇到过什么 bug？** | ① 环境变量 ELECTRON_RUN_AS_NODE=1 导致 Electron 被当普通 Node 启动、require('electron') 返回异常——从崩溃日志逐层定位；② 导出 CSV 被 Excel 自动改日期显示格式——用 `="YYYY-MM-DD"` 文本公式固定；③ 文件被 Excel 占用导致写入 EBUSY——捕获错误码转友好提示 |
| **你写了多少代码？** | 诚实答：代码由 Claude Code 生成，我的工作在设计方案、分阶段验收、缺陷修复推动；但每个模块我都讲得清（然后主动讲上面任一细节） |
| **怎么保证 AI 代码质量？** | 小步分阶段交付 + 每阶段人工验收测试 + 缺陷反馈闭环；TypeScript 类型检查作提交门禁 |

---

## 5. 一句话自我介绍（面试开场可用）

> "计算机专业，能独立用 AI 工具完成从需求到发布的完整软件交付，并且讲得清每一处技术细节。"

---

## 6. 可选加分项（秋招时间紧，按优先级挑）

- [ ] **GitHub 开源 + README**（我可以帮你写好，简历放链接）——性价比最高
- [ ] 把第 4 节技术点逐个对照源码看懂（我可以当"答疑老师"，随时问）
- [ ] 学 Vue 3 基础语法（我可以开小课堂，半天能上手）
- [ ] 给 APP 加一个功能（如"预算提醒"），简历写"持续迭代中"，面试聊迭代思路
